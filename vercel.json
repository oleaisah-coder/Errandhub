{
  "buildCommand": "cd app && npm install --ignore-scripts && npm run build && cp public/onboarding.js dist/",
  "installCommand": "cd app && npm install --ignore-scripts",
  "outputDirectory": "app/dist"
}